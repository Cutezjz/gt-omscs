'use strict';

console.log([
  'gulp: Runs linters against source code.',
  'gulp watch: Watches for changes in source code and kicks off linters.',
  'gulp test: Runs unit tests.',
  'gulp complexity: Runs complexity analysis tests.',
  ''
].join('\n'));

process.exit();
